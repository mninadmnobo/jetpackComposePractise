# Jetpack with Retrofit

This covers simple navigation with jetpack compose and api calling with retrofit


### How to run
For backend, 

```
cd backend
npm install
npm start
```

For frontend, run with android studio (emulator)
